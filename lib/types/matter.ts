/**
 * Re-exports of the shape types used by the matter workspace. The types
 * are declared alongside the canonical demo dataset in `lib/mock`; this
 * module gives pages a stable import path so they don't reach into mock.
 */
export type { DocSlot, SlotTemplate, Blocker, MatterWorkspace } from "@/lib/mock";
